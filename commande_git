## steps en details de comment se connecter et continuer a travailler sur un repositery cree manuellement sur github

- ouvrir vs code (se placer dans un dossier ou vous souhaitez mettre votre dossier du projet)
- ouvrir terminal
- suivre les dommandes suivantes

## 1. Se déconnecter du repository actuel

```bash
# Vérifie ton remote actuel
git clone https://github.com/kikjabouza/Izil.git
```

## 2. Se connecter au nouveau repository

```bash
# Ajouter le nouveau remote
git remote add origin [url_du_nouveau_repo]
```

## 3. cloner : c.a.d recuperer tout le contenu d'un dossier sur un repositery

```bash
# Ajouter le nouveau remote
git remote add origin [url_du_nouveau_repo]
```

## 4. Travailler avec la branche "Khadija"

```bash
# Se déplacer vers la branche Khadija !!!!
git checkout Khadija
git branch

# Si la branche n'existe pas encore localement !!!!
git checkout -b Khadija
```

## 5. Pusher tes changements

```bash
# Ajouter tes fichiers
git add .

# Faire un commit
git commit -m "Ton message de commit"

# Pousser vers la branche Khadija
git push -u origin khadija
```

## 6. Recuperer les changements des autres

```bash
git pull

```
